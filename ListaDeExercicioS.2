// 2-Escreva um programa que recebe três números e determina qual é o maior deles, usando if, else if e else.

let n = 4;
let m = 8;
let o = 3;

if (o>m || o>n) {
    alert(o+" É o maior numero");
}
else if (m>n || m>o) {
    alert (m+ " É o maior");
}

else{ (n>o || n>m)
    alert(n+ " É o maior numero entre eles");
}