// 3-Faça um programa que verifica se uma senha armazenada na variável SENHA é válida. A senha é válida se tiver pelo menos 8 caracteres e conter pelo menos uma letra maiúscula, uma letra minúscula e um número.

var UserPass = parseInt(prompt("Digite uma senha:"));

if (UserPass == a234567B) {
    ALERT("senha correta");
}else{
    alert("senha inválida);
}